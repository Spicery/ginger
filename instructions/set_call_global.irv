Ref r = ((IdentClass *)( pc[ 2 ] )) -> valof;
VMCOUNT = ToLong( pc[ 1 ] );
{    
    Ref *ptr;
    if ( !IsPtr4( r ) ) call_error( r );
    ptr = RefToPtr4( r );
    if ( !IsFunction( *ptr ) ) call_error( r );
    VMLINK = pc + 3;
    RETURN( ptr + 1 );
}
