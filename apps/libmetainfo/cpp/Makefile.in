prefix=@prefix@
exec_prefix=@prefix@
datarootdir=@datarootdir@
PACKAGE_TARNAME=@PACKAGE_TARNAME@
INSTALL_BIN=@bindir@
INSTALL_LIB=@datarootdir@/@PACKAGE_TARNAME@
INSTALL_DOC=@docdir@

EXEC_DIR="$(INSTALL_BIN)"

CC=@CXX@

CXXFLAGS=-DINSTALL_BIN='"$(INSTALL_BIN)"' -DINSTALL_LIB='"$(INSTALL_LIB)"' -DEXEC_DIR='$(EXEC_DIR)' -g -I../hpp -I../../libgng/hpp -I/opt/local/include -Wall -Wnon-virtual-dtor # -Weffc++
CFLAGS=-Wmissing-prototypes 

OBJS=metainfo.o

all: libmetainfo.a

# $@ is make shorthand for the target.
# $@ is make shorthand for the dependencies
# Note that calling ranlib should not be needed but I am seeing some weird stuff
libmetainfo.a: $(OBJS)
	ar -rcs $@ $+
	ranlib $@
	
clean:
	rm -f *.o
	rm -f *.a

#	Nothing needs doing.
check:
