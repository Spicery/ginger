all:
	@echo "Valid targets are:"
	@echo "  build - builds all artefacts"
	@echo "  clean - resets to initial state"
	@echo "  check - will run all tests incl. unit, component & post installation checks"

build:
	make -C cpp $@

clean:
	make -C cpp $@
	# make -C unittest $@

check:
	# make -C unittest $@
