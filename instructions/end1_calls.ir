Ref r = *( VMVP-- );
long var = ToLong( pc[ 1 ] );
VMCOUNT = ( VMVP - ( Ref * )( VMSP[ var ] ) ) - 1;
{    
    Ref *ptr;
    if ( !IsPtr4( r ) ) call_error( r );
    ptr = RefToPtr4( r );
    if ( !IsFnKey( *ptr ) ) call_error( r );
    VMLINK = pc + 2;
    RETURN( ptr + 1 );
}
